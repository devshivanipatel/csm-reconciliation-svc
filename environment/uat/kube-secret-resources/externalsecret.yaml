apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: csm-route-svc-secrets
  namespace: csm-uat-namespace
  labels:
    app: csm-route-svc
    type: backend
    application: csm
    environment: uat
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: csm-external-secrets
    kind: SecretStore
  target:
    name: csm-route-svc-secrets
    creationPolicy: Owner
  data:
    - secretKey: DB_USER
      remoteRef:
        key: osttra-csm-uat-postgres-username
        #property: username #AWS Secrets Manager secret key
    - secretKey: DB_PASS
      remoteRef:
        key: osttra-csm-uat-postgres-password
        #property: username #AWS Secrets Manager secret key
    - secretKey: JWT_SECRET_KEY
      remoteRef:
        key: osttra-csm-jwt-secret
    - secretKey: COGNITO_CLIENT_ID
      remoteRef:
        key: osttra-csm-uat-cognito_client_id
    - secretKey: COGNITO_CLIENT_SECRET
      remoteRef:
        key: osttra-csm-uat-cognito_client_secret
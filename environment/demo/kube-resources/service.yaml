apiVersion: v1
kind: Service
metadata:
  name: csm-reconciliation-svc
  namespace: csm-demo-namespace
  labels:
    environment: demo
    application: csm
spec:
  ports:
    - port: 80
      targetPort: 8090
      protocol: TCP
  selector:
    app: csm-reconciliation-svc
  type: NodePort

apiVersion: v1
kind: Service
metadata:
  name: csm-reconciliation-svc
  namespace: csm-dev-namespace
  labels:
    environment: dev
    application: csm
spec:
  ports:
    - port: 80
      targetPort: 8090
      protocol: TCP
  selector:
    app: csm-reconciliation-svc
  type: NodePort
